import { Button, HStack } from "@chakra-ui/react"

const SideBar = () => {
    return (
        <div>
            <HStack>
                <Button>Click me</Button>
                <Button>Click me</Button>
            </HStack>
        </div>
    )
}

export default SideBar;